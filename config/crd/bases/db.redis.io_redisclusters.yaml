---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: redisclusters.db.redis.io
spec:
  group: db.redis.io
  names:
    kind: RedisCluster
    listKind: RedisClusterList
    plural: redisclusters
    singular: rediscluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RedisCluster is the Schema for the redisclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RedisCluster
            properties:
              enableSentinel:
                description: EnableSentinel 是否启用哨兵模式
                type: boolean
              image:
                description: Image Redis 镜像版本
                type: string
              password:
                description: Password Redis 认证密码
                type: string
              port:
                description: Port Redis 服务端口
                format: int32
                type: integer
              replicas:
                description: Replicas 定义集群节点数量
                format: int32
                minimum: 1
                type: integer
              sentinelConfig:
                description: SentinelConfig 哨兵模式详细配置（仅在 EnableSentinel 为 true 时生效）
                properties:
                  downAfterMilliseconds:
                    default: 5000
                    description: DownAfterMilliseconds 毫秒级，主节点无响应超时时间
                    format: int32
                    minimum: 1000
                    type: integer
                  failoverTimeoutMilliseconds:
                    default: 10000
                    description: FailoverTimeoutMilliseconds 毫秒级，故障转移超时时间
                    format: int32
                    minimum: 1000
                    type: integer
                  password:
                    description: Password Sentinel 认证密码（如果 Redis 设置了密码）
                    type: string
                  port:
                    default: 26379
                    description: Port Sentinel 服务端口
                    format: int32
                    maximum: 65535
                    minimum: 1024
                    type: integer
                  quorum:
                    default: 2
                    description: Quorum 判断主节点下线所需的投票数
                    format: int32
                    minimum: 1
                    type: integer
                  replicas:
                    default: 3
                    description: Replicas 定义 Sentinel 副本数量
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              storageSize:
                description: StorageSize 存储卷大小
                type: string
            type: object
          status:
            description: status defines the observed state of RedisCluster
            properties:
              readyReplicas:
                description: ReadyReplicas 当前就绪的副本数量
                format: int32
                type: integer
              state:
                description: State 集群状态
                type: string
            required:
            - readyReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
